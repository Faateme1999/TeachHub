# Copy this file to ".env" and adjust if needed:
#   cp .env.example .env
#
# These defaults match the database defined in docker-compose.yml
# (user "teachhub", password "teachhub", database "teachhub", host port 5433).
# If you start the DB with `docker compose up -d`, this works as-is.


DATABASE_URL="postgresql://teachhub:teachhub@localhost:5433/teachhub"
# DATABASE_URL="postgresql://...neon.tech/...?sslmode=require&channel_binding=require"
# DATABASE_URL="postgresql://neondb_owner:npg_YVnkmhCbWE63@ep-muddy-art-b1r1nory.c-5.eu-central-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require&connect_timeout=30"

FRONTEND_URL="http://localhost:5173"
PORT=3000
# The secret used to sign JWT login tokens.
# Payload + JWT Secret => Create Token
JWT_SECRET=